# Backend runtime dependencies for the AgriSmartSuite Flask API.
# numpy and scikit-learn are pinned to the versions the pickled models were
# trained/serialized with — changing them can break model loading.
Flask==3.1.0
flask-cors==5.0.1
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.3.2
scipy==1.15.3
joblib==1.5.0

# Testing
pytest==8.3.4
