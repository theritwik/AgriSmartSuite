# Copy to .env and adjust as needed. Consumed by app.py at startup.
# Interface to bind the Flask server to (use 0.0.0.0 to expose on your network).
FLASK_HOST=127.0.0.1
# Port to listen on.
FLASK_PORT=5000
# Enable Flask's debugger and auto-reloader. Keep "false" in production.
FLASK_DEBUG=false
